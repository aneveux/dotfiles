#      |\__/,|   (`\
#      |o o  |__ _)
#    _.( T   )  `  /
#   ((_ `^--' /_<  \
#   `` `-'(((/  (((/
# .gitignore

# because that's where you can keep all your fancy secret things
local

# because that's generated by Ansible when things are going wrong
*.retry

# I did that for a really good reason which I sadly forgot.
fish_prompt.fish
